# apiVersion: v1
# kind: Pod
# metadata: 
#   name: nginx
#   labels:
#     app: nginx
#     tier: frontend

# spec:
#   containers:
#     - name: nginx
#       image: nginx
#     - name: busybox
#       image: busybox
#       command: ["sleep", "3600"]


apiVersion: v1
kind: Pod
metadata: 
  name: pod1
  labels:
    app: name
spec:
  containers:
    - image: nginx
      name: nginx-container
    - image: busybox
      name: busybox-container