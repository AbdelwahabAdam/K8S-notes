apiVersion: v1
kind: Service
metadata:
  name: master
spec:
  type: NodePort
  selector:
    app: master
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
    - protocol: TCP
      port: 8080
      targetPort: 8080


