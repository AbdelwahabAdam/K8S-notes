apiVersion: v1
kind: Service
metadata:
  name: vote
  labels:
    app: vote
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 80
      name: "vote-service"
      nodePort: 31000
  selector:
    app: vote